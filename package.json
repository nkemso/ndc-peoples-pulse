{
  "name": "ndc-peoples-pulse",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "frontend-dev": "cd frontend && npm run dev",
    "backend-dev": "cd backend && npm run dev",
    "install-all": "npm install && cd frontend && npm install && cd ../backend && npm install"
  }
}
